<%= form_with(model: phase, local: true) do |form| %>
  <% if phase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phase.errors.count, "error") %> prohibited this phase from being saved:</h2>

      <ul>
        <% phase.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= form.text_field :name, :class => "form-control", readonly: true %>
  </div>

  <br />

  <div class="field">
    <%= form.label :start_date , "Fecha de inicio", :class => "form-label" %>
    <%= form.date_select :start_date, :class => "form-control" %>
  </div>

  <br />

  <div class="field">
    <%= form.label :end_date, "Fecha de termino", :class => "form-label" %>
    <%= form.date_select :end_date, :class => "form-control" %>
  </div>

  <div>



  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
