<%= form_with(model: academic_level, local: true) do |form| %>
  <% if academic_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academic_level.errors.count, "error") %> prohibited this academic_level from being saved:</h2>

      <ul>
        <% academic_level.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
