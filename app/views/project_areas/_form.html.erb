<%= form_with(model: project_area, local: true) do |form| %>
  <% if project_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_area.errors.count, "error") %> prohibited this project_area from being saved:</h2>

      <ul>
        <% project_area.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
