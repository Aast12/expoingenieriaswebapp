<%= form_with(model: operative, local: true) do |form| %>
  <% if operative.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(operative.errors.count, "error") %> prohibited this operative from being saved:</h2>

      <ul>
        <% operative.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
